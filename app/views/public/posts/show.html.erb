<h2>投稿詳細</h2>


<div class="container">
<div class="row">
<div class="col-5">
</div>
<div class="col-4">

タイトル <%= @post.title %></br>

説明 <%= @post.body %> <br>

<p>画像<br>
<%= image_tag @post.get_post_image, class:"post-index-img" %>

<% if @post.genres.any? %>
  <% @post.genres.each do |genre| %>
    <%= genre.name %>
  <% end %>
<% end %>

コメントをする

<%= form_with(model:[@post, @comment], url: public_comments_path, method: :post) do |f| %>
  <%= f.text_area :comment_content %>
  <%= f.hidden_field :post_id, value: @post.id %>
  <%= f.submit 'コメントする' %>
  <% end %>

 コメント一覧

<% @comments.each do |comment| %>
  <%= comment.comment_content %>
<% end %>

<h3> <%= @post.likes.count %> いいね</h3>
<% if customer_signed_in? && current_customer.already_liked?(@post) %>
  <%= link_to public_post_like_path(@post), method: :delete, class: "unlike-btn" do %>

  <i class="fas fa-heart"></i>
   <% end %>
<% else %>
  <%= link_to public_post_likes_path(@post),method: :post, class: "like-btn" do %>
   <i class="far fa-heart"></i>
   <% end %>
<% end %>
<h2>いいねしたユーザー</h2>
<% @post.liked_customers.each do |customer| %>
  <li><%= customer.nickname %></li>
<% end %>


<% if customer_signed_in? && current_customer.id == @post.customer_id %>
<%= link_to '編集する',edit_public_post_path, class: "btn btn-success" %>
 <%= link_to "削除する", public_post_path(@post), method: :delete,data: { confirm: '本当に消しますか？' },class: "btn btn-danger" %>
 <% end %>
</div>
</div>
</div>